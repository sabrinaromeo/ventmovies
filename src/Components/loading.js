
import ClipLoader from "react-spinners/ClipLoader";

const Loading = () => {
    return ( 
            <ClipLoader color={"blue"} size={88} css={{display:'block', margin: '0 auto'}}/>

     );
}
 
export default Loading;